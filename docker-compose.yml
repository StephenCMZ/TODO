services:
  todo:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DB_PATH=/app/data/todo.db
      - DB_PASSWORD=${DB_PASSWORD:-}
      - PORT=3001
    volumes:
      - ./data:/app/data
